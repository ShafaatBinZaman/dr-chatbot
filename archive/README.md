# Archive

`dr_chatbot_prototype/` is an earlier Flutter client built around a login/signup choice
screen and direct Firestore access. It was superseded by `app/`, which talks to the
Flask backend over HTTP. Kept here for reference and for the project report; it is not
built or maintained.
